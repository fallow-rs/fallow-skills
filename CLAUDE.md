# Fallow public skills adapter for Claude

Read `@AGENTS.md` first. It is the shared repository contract for Claude,
Codex, and contributors.

Claude plugin metadata lives under `.claude-plugin/` and
`fallow/.claude-plugin/`. The authored skill content remains under
`fallow/skills/`; do not create a second Claude-specific copy.
